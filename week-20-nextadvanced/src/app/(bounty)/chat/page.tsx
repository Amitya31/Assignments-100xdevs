import React from 'react'

function Chat() {
  return (
    <div>Chat</div>
    //The () parenthesis tells nextjs to ignore the route
    // This is called route group so bounty is route group
  )
}

export default Chat